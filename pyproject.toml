[tool.poetry]
name = "termquotes"
version = "0.1.0"
description = ""
authors = ["Titus Soporan <titus@tsoporan.com>"]

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]
pytest = "^3.0"

[tool.poetry.scripts]
termquotes = 'termquotes.main:run'

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
